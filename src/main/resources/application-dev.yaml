spring:
  datasource:
    url: jdbc:mysql://localhost:2211/chalet_db

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.flywaydb: DEBUG

management:
  endpoint:
    health:
      show-details: always
